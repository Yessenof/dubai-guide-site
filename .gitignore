# Dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js
/.next/
/out/

# Build outputs
/build
dist

# Environment variables
.env
.env.local
.env.*.local
.env*

# Test coverage
/coverage

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.log
*.tmp
*.swp
*.swo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Certificates
*.pem

# SQLite — main DB and WAL auxiliary files
data/guides.db
data/guides.db-shm
data/guides.db-wal
data/guides.db.backup-*

# SQLite — freshness DB (isolated, never guides.db) and WAL auxiliary files
data/freshness.db
data/freshness.db-shm
data/freshness.db-wal
data/freshness.db.backup-*

# Production DB backups (local copies pulled from Cloudways)
# Directory is tracked via .gitkeep; actual backup files are not committed.
backups/production-db/*.db*

# IDEs
.idea
.vscode/*
!.vscode/extensions.json
